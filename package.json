{
  "name": "local-story-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Local Story Hub - prototype + tests",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
