// Template — copy this file to "ai-assist-config.js" (gitignored + excluded from
// `firebase deploy` via firebase.json hosting.ignore) and fill in your real key.
// Get a key at https://openrouter.ai/keys — never commit the real file, and never
// remove "ai-assist-config.js" from firebase.json's hosting.ignore list.
window.OPENROUTER_CONFIG = {
  apiKey: "sk-or-v1-YOUR_REAL_KEY_HERE",
  model: "google/gemini-2.5-flash-lite",
};
