// Template — copy this file to "ai-test-config.js" (gitignored) and fill in your real key.
// Get a key at https://openrouter.ai/keys — never commit the real file.
window.OPENROUTER_CONFIG = {
  apiKey: "sk-or-v1-YOUR_REAL_KEY_HERE",
  model: "google/gemini-2.5-flash-lite",
};
